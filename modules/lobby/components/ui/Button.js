import { Button as BaseButton } from '@/themes/base/components/ui/buttons';
// const Button = ({ children, size="md", className, ...props }) => {
//   const classNames = [];
//   if (className) {
//     classNames.push(className);
//   }
//   if (size === "md") {
//     classNames.push("px-3 py-1 text-sm");
//   }
//   return (
//     <button className={`lobby-button ${classNames.join(' ')}`} {...props}>
//       {children}
//     </button>
//   );
// };

export default BaseButton;