'use client';
import GridLayout from '@/modules/lobby/components/layouts/GridLayout';

export default function Home() {
  // const theme = useSelector((s) => s.lobby.theme);
  return <GridLayout />;
}
